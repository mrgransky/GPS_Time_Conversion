# GPS_Time_Conversion
gps_conversion to Unix:

c++ implementation

reference:

https://www.andrews.edu/~tzs/timeconv/timeconvert.php


## compile:

$ mkdir build && cd build

$ cmake ..

$ make

## run:

$ cd build

$ ./gps2unix "GPS_TIME"
